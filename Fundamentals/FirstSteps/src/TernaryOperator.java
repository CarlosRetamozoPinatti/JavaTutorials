public class TernaryOperator {
    public static void main(String[] args) {
        //Here we evaluate a boolean value. If true, we use "?" to assign the action. If false, we use ":" to assign the action.
        var result = (3 < 2) ? "Its True" : "Its False";
        System.out.println("result = " + result);
    }
}
