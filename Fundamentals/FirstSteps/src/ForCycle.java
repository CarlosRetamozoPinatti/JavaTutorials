//For Cycles performs a fixed amount of iterations, until the counter reach its final value, executing the code written on every iteration.

public class ForCycle {
    public static void main(String[] args) {
        for(var counter = 0; counter < 5; counter++){
            System.out.println("counter = " + counter);
        }
    }
}
