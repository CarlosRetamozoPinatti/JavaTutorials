package enumeration;
//An enumeration is a special class, which contains various elements inside.
//All the elements inside an enumeration are constants, which mean they are public, static, and final.
public enum Days {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}
